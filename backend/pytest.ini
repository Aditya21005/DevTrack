[pytest]
testpaths = tests
asyncio_mode = auto
pythonpath = .
addopts = -q --strict-markers
markers =
    integration: cross-module or external-boundary tests
    api: FastAPI route contract tests
    auth: authentication and authorization tests